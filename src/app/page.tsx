import { NovaAnaliseFormulario } from '@/features/analises/components/nova-analise-formulario'

export default function Home() {
  return (
    <main className="flex min-h-full flex-1 items-center justify-center px-6 py-16 sm:px-10">
      <NovaAnaliseFormulario />
    </main>
  );
}
